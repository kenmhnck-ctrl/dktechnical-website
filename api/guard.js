export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST only'});const {action='',approved=false,connected=false,hasConsent=false}=req.body||{};const outbound=['email_send','social_publish','google_publish'];if(outbound.includes(action)&&!approved)return res.status(200).json({allowed:false,reason:'APPROVAL_REQUIRED'});if(outbound.includes(action)&&!connected)return res.status(200).json({allowed:false,reason:'CHANNEL_NOT_CONNECTED'});if(action==='email_send'&&!hasConsent)return res.status(200).json({allowed:false,reason:'CONSENT_OR_VALID_BASIS_REQUIRED'});return res.status(200).json({allowed:true,action,policy:'ORDNIX_SAFE_AUTOMATION'});}