export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST only'});const {approved=false,channel='SEO',assetId='asset-demo',schedule='next_available'}=req.body||{};if(!approved)return res.status(200).json({assetId,status:'BLOCKED',reason:'HUMAN_APPROVAL_REQUIRED'});return res.status(200).json({assetId,status:'QUEUED',channel,schedule,deliveryMode:'CONNECTOR_REQUIRED',message:'承認済み。正規チャネル接続後に配信可能です。'});}