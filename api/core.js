export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST only'});const q=req.query||{};const action=q.action||(req.body&&req.body.action)||'';const b=req.body||{};const company=(b.company||'御社').trim();const industry=b.industry||'その他';const region=b.region||'全国';const service=b.service||'サービス';const goal=b.goal||'問い合わせを増やす';const channels={'建設・設備':['Google検索','施工事例SEO','見積追客'],'美容・サロン':['Instagram','Googleマップ','再来店追客'],'不動産':['SEO','査定LP','追客メール'],'士業・コンサル':['専門記事SEO','相談LP','見込み客育成'],'医療・クリニック':['地域SEO','Googleマップ','予約導線'],'自動車・整備':['Google検索','車検LP','定期フォロー'],'飲食・店舗':['Googleマップ','SNS','再来店施策'],'その他':['SEO','SNS','追客']};const c=channels[industry]||channels['その他'];if(action==='diagnose'){const channel=b.channel||'紹介中心',problem=b.problem||'問い合わせが少ない';let score=55;if(channel==='ほぼ何もしていない')score-=10;if(problem==='追客できていない')score-=8;if(channel==='紹介中心')score-=5;if(channel==='広告')score+=7;if(channel==='SNS'||channel==='ホームページ')score+=5;score=Math.max(28,Math.min(82,score));return res.status(200).json({score,summary:`${company}は「${channel}」が中心で、主な課題は「${problem}」。最優先は、集客入口・問い合わせ導線・追客の3点を一つの流れにすることです。`,actions:c,nextStep:'90日集客プランを生成'});}if(action==='plan'){const weeks=Array.from({length:12},(_,i)=>({week:i+1,focus:i<4?'基盤構築':i<8?'集客拡大':'改善・最適化',tasks:[`${c[0]}向けコンテンツを作成`,`${c[1]}の導線を改善`,`${c[2]}を実行・見直し`]}));return res.status(200).json({goal,industry,days:90,channels:c,weeks,today:[`${c[0]}用コンテンツ1本を作成`,`SNS/配信用コピー3件を生成`,`未対応リードを抽出し追客文を生成`,`LP改善案を2件作成`,`今週のKPIを確認`]});}if(action==='autopilot'){const today=[{type:'CONTENT',channel:c[0],title:`${industry}向け：${service}で失敗しない3つのポイント`,status:'READY_FOR_REVIEW'},{type:'SOCIAL',channel:c[1],title:`${company}の強みを伝える短文投稿を3本生成`,status:'READY_FOR_REVIEW'},{type:'SEO',channel:'Website',title:`「${region} ${industry} ${service}」の検索意図に合わせた記事構成を作成`,status:'READY_FOR_REVIEW'},{type:'FOLLOWUP',channel:'CRM',title:'新規リードへの24時間・3日・7日追客シナリオを準備',status:'READY_FOR_REVIEW'},{type:'OPTIMIZE',channel:'ORDNIX',title:`目標「${goal}」に対する週次KPIを設定`,status:'ACTIVE'}];return res.status(200).json({mode:'ORDNIX_AUTOPILOT',company,industry,region,channels:c,today,nextReview:'7 days',humanApprovalRequired:true});}if(action==='content'){const base=`${region}で${service}を検討している方へ。${company}では、分かりやすくご案内しています。`;return res.status(200).json({status:'READY_FOR_REVIEW',assets:{seo:{title:`${region}の${industry}｜${service}なら${company}`,description:`${service}について、選び方・注意点・相談方法を解説。${company}がサポートします。`,outline:['よくある悩み','選び方のポイント','失敗しないための注意点','相談から利用までの流れ','よくある質問']},social:[`${base} 気になる点があればお気軽にご相談ください。`,`${service}選びで迷っていませんか？ ${company}が大切にしているポイントを3つにまとめました。`,`${company}の強みを短く分かりやすくご紹介します。`],google:`${company}からのお知らせ：${service}に関するご相談を受け付けています。${region}でお困りの方はお気軽にお問い合わせください。`}});}if(action==='followup'){const stage=b.stage||'new';const seq=stage==='engaged'?[{day:0,subject:'御社向け90日プラン',body:'御社向けに90日間の実行プランを準備できます。'},{day:2,subject:'導入後の動き',body:'初期設定後は毎週の実行内容と改善内容をダッシュボードで確認できます。'}]:[{day:0,subject:'AI集客診断の結果について',body:`${company}様の診断結果をもとに、優先改善項目を3つに整理しました。`},{day:1,subject:'最初に変えるならここです',body:`${industry}では問い合わせ導線を先に整える方が効果を測りやすくなります。`},{day:3,subject:'ORDNIXで自動化できる範囲',body:`${goal}に向けて、発信・リード管理・追客・改善までを一つの流れにできます。`},{day:7,subject:'7日後のご確認',body:'診断内容について不明点があればご確認ください。'}];return res.status(200).json({stage,sequence:seq,sendMode:'DRAFT_UNTIL_CHANNEL_CONNECTED',unsubscribeRequired:true});}if(action==='optimize'){const visits=+b.visits||0,leads=+b.leads||0,meetings=+b.meetings||0,contracts=+b.contracts||0,spend=+b.spend||0;const lr=visits?leads/visits:0,mr=leads?meetings/leads:0,cr=meetings?contracts/meetings:0;let priority='TRAFFIC',next='SEO・SNS・広告クリエイティブを増やす';if(visits>=100&&lr<.08){priority='CONVERSION';next='LPの見出し・無料診断CTA・入力項目を改善する'}else if(leads>=10&&mr<.3){priority='NURTURE';next='診断結果と追客シナリオを改善する'}else if(meetings>=5&&cr<.2){priority='OFFER';next='料金・導入メリット・リスク低減策を改善する'}else if(contracts>=1){priority='SCALE';next='最も成約率の高い業界・チャネルへ集中する'}return res.status(200).json({metrics:{visits,leads,meetings,contracts,spend,leadRate:+(lr*100).toFixed(1),meetingRate:+(mr*100).toFixed(1),closeRate:+(cr*100).toFixed(1),cac:contracts?spend/contracts:null},priority,action:next,nextReview:'7 days'});}if(action==='approval'){const x=b.action||'approve';const status=x==='approve'?'APPROVED':x==='reject'?'REJECTED':'REVISION_REQUESTED';return res.status(200).json({assetId:b.assetId||'asset-demo',status,publishEligibility:status==='APPROVED',timestamp:new Date().toISOString()});}if(action==='crm'){const stages={diagnosis:'NEW',opened:'ENGAGED',consultation:'MEETING',proposal:'PROPOSAL',contract:'WON',lost:'LOST'};const stage=stages[b.event]||'NEW',score=+b.score||0;return res.status(200).json({lead:{...(b.lead||{}),stage,priority:score>=70?'HIGH':score>=50?'MEDIUM':'NORMAL',score,updatedAt:new Date().toISOString()}});}if(action==='report'){const visits=+b.visits||0,leads=+b.leads||0,meetings=+b.meetings||0,contracts=+b.contracts||0;return res.status(200).json({company,summary:`今週は${leads}件のリード、${meetings}件の商談、${contracts}件の契約。`,kpis:{visits,leads,meetings,contracts},nextFocus:contracts>0?'勝ちチャネルへ集中':leads>0?'診断→相談への転換改善':'流入を増やす'});}if(action==='onboarding'){return res.status(200).json({company,industry,region,service,goal,status:'READY',steps:['会社情報確認','ターゲット設定','90日計画生成','チャネル接続','初回コンテンツ承認']});}return res.status(400).json({error:'unknown action'});}